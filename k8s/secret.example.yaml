apiVersion: v1
kind: Secret
metadata:
  name: edc-validation-dashboard-secret
type: Opaque
stringData:
  # Replace before deployment. The public MVD demo default is "password".
  MVD_API_KEY_VALUE: "replace-me"
